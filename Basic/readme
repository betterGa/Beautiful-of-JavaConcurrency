✨ sleep() 方法 和 yield() 方法的区别:
    sleep()方法是使线程进入阻塞挂起状态，在这期间线程调度器不会去调度该线程，
    而 yield() 是使线程进入就绪状态（线程只是让出自己剩余的时间片，并没有被阻塞挂起，
    而是处于就绪状态，线程调度器下一次调度时就有可能调度到当前线程执行）。


✨ 会抛InterruptedException异常的方法：
    wait()、join()、sleep()。
